import { BaseTextInput } from '.';

const meta = {
  title: 'BaseTextInput',
  component: BaseTextInput,
};

export default meta;

export const BaseTextInputStory = {
  args: {},
  parameters: {},
};
