import { StyleSheet } from 'react-native';

import { spacings } from '../../../../shared/styles/globalStyles';

export const routineManagerButtonsStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: spacings.sm,
  },
});
