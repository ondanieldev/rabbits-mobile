import { StyleSheet } from 'react-native';

import { colors } from '../../../../shared/styles/globalStyles';

export const preferenceViewStyles = StyleSheet.create({
  container: {
    backgroundColor: colors.foreground,
  },
});
